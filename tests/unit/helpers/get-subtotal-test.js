import {
  getSubtotal
} from 'toolz-app/helpers/get-subtotal';

module('GetSubtotalHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = getSubtotal(42);
  ok(result);
});
